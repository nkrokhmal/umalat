Milk project department no longer exists. Now milk projects contains adygea and brynza. 

`parse_schedule.py` and `to_boiling_plan.py` are kept because of legacy dependencies and will be deleted in the future.